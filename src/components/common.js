const HOST = "http://localhost"
const PORT = 3003

export const LOGIN_URL = `${HOST}:${PORT}/login`
export const REGISTER_URL = `${HOST}:${PORT}/register`
export const INFO_URL = `${HOST}:${PORT}/info`